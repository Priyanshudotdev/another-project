// NextAuth removed. Placeholder module retained to avoid broken imports.
// If any part of the app still imports from "@/lib/auth/config", it will not crash.
export const authOptions = undefined as never;
export default {};
